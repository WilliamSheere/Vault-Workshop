// Inputting lock combination
// Create a varible that = 10 using addition
// Create a variable that = 39 using subtraction
// Create a variable that = 40 using multiplication

//Step 1 Create message
const lock =
  "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
console.log(lock);
alert(lock);
// Step 2 create variables.
const lock1 = 5 + 5;
const lock2 = 50 - 11;
const lock3 = 20 * 2;
const combo = `${lock1}, ${lock2}, ${lock3}`;
alert(combo);
